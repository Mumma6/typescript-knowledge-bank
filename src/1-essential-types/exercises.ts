// Exercises covering all topics in Section 1
// 1-1 Basic Types
// Exercise 1: Define a variable called `userName` that holds a string, and a variable `userAge` that holds a number.

// Exercise 2: Create a tuple `personInfo` containing a string (name), a number (age), and a boolean (isDeveloper).

// Exercise 3: Create a union type variable `statusCode` that can be a number or a string.

// 1-2 Interfaces and Type Aliases
// Exercise 4: Define an interface `Product` with properties `name` (string), `price` (number), and optional `category` (string).

// Exercise 5: Define a type alias `ID` that can be either a string or a number.

// 1-3 Type Annotations
// Exercise 6: Write a function `greet` that takes a name (string) and age (number) as arguments and returns a string.

// 1-4 Literal Types
// Exercise 7: Create a literal type `Env` that can be 'development', 'production', or 'test'.
// Use it in a function that accepts this type.

// 1-5 Utility Types
// Exercise 8: Use the `Partial` utility type to create a partial version of the `Product` interface from Exercise 4.

// Exercise 9: Use the `Pick` utility type to create a type that includes only the `name` and `price` from `Product`.
